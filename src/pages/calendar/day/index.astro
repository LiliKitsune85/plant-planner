---
import { formatDateInTimezone } from '@/lib/utils/timezone'

export const prerender = false

const timezone = Astro.locals.profileTimezone ?? 'UTC'
const targetDate = formatDateInTimezone(new Date(), timezone)
return Astro.redirect(`/calendar/day/${targetDate}`)
---
