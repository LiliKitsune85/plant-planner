---
import { formatDateInTimezone } from "@/lib/utils/timezone";

export const prerender = false;

const timezone = Astro.locals.profileTimezone ?? "UTC";
const targetMonth = formatDateInTimezone(new Date(), timezone).slice(0, 7);
return Astro.redirect(`/calendar/${targetMonth}`);
---
